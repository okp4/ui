import { ArgsTable, Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs'
import { Logo } from 'ui/atoms/Logo/logo'

<Meta
  title="Atoms/Logo"
  component={Logo}
  parameters={{
    actions: { argTypesRegex: '^on.*' }
  }}
/>

# Logo

> Represents the brand of OKP4.

[![stability-unstable](https://img.shields.io/badge/stability-unstable-yellow.svg)](https://github.com/emersion/stability-badges#unstable)

## Overview

The logo helps our users identify our brand when using our services. It must have the best possible readability.

---

<Canvas>
  <Story
    name="Overview"
    argTypes={{}}
    args={{
      variant: 'light',
      size: 'small',
      type: 'logo'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          padding: '20px',
          background:
            args.variant === 'light'
              ? 'transparent linear-gradient(180deg, #faf8f4 0%, white 100%) 0% 0% no-repeat padding-box'
              : 'transparent linear-gradient(180deg, #0c1529 0%, #0f224a 100%) 0% 0% no-repeat padding-box'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

---

### Properties

<ArgsTable story="Overview" />

## Sizes

The logo comes with three different sizes to allow its use according to its location within the interface.

- **Small**: particularly suitable for small screens and must not saturate the space. This size should also be used for integrations in content spaces such as cards, footers, etc...
- **Medium**: makes it possible to adapt to a greater number of uses, both to represent the brand and to be used in content elements.
- **Large**: particularly allows to affix the OKP4 brand within an interface and to set the tone of the visual used. Should not be used in content elements.

<Canvas>
  <Story
    name="Size - Small"
    args={{
      type: 'logo',
      variant: 'light',
      size: 'small'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Size - Medium"
    args={{
      type: 'logo',
      variant: 'light',
      size: 'medium'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Size - Large"
    args={{
      type: 'logo',
      variant: 'light',
      size: 'large'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

## Variants

Variants make it possible to mark the use of OKP4 logos within an ensemble. The contrasts are important in order to ensure maximum integration in the interfaces.

- **Dark**: this is the reference variant of the logo, which makes it possible to magnify the use of the Blue Stone theme by using in particular a high contrast of white.
- **Light**: the secondary variant, making it possible to integrate harmoniously into an Extra Light theme and also to affirm the OKP4 brand through the use of blue.

<Canvas>
  <Story
    name="Variant - Dark"
    args={{
      type: 'logo',
      variant: 'dark',
      size: 'medium'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          background:
            'transparent linear-gradient(180deg, #0C1529 0%, #0F224A 100%) 0% 0% no-repeat padding-box',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Variant - Light"
    args={{
      type: 'logo',
      variant: 'light',
      size: 'medium'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          background:
            'transparent linear-gradient(180deg, #FAF8F4 0%, #FFFFFF 100%) 0% 0% no-repeat padding-box',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

## Types

The logo comes with a property called `type` which allows to directly use the standalone logo or to freely compose destructured logo's elements.

- **Logo**: the standalone logo, mainly designed to mark the visual identity of OKP4. Can be used in a header for example.
- **Logotype**: has an immediate visual impact on the perception of the company, can be used in particular in cards or as a favicon.
- **Logomark**: represents the OKP4 name in order to mark the membership and not to weigh down the interface by being smoothed with the elements of content.
- **Slogan**: the slogan is used to add strength to OKP4 content, especially in terms of protocol usage. It can be used in presentation elements to assert the visual impact of the interface.

<Canvas>
  <Story
    name="Type - Logo"
    args={{
      type: 'logo',
      variant: 'light',
      size: 'medium'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Type - Logotype"
    args={{
      type: 'logotype',
      variant: 'light',
      size: 'medium'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Type - Logomark"
    args={{
      type: 'logomark',
      variant: 'light',
      size: 'medium'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Type - Slogan"
    args={{
      type: 'slogan',
      variant: 'light',
      size: 'medium'
    }}
  >
    {args => (
      <div
        style={{
          textAlign: 'center',
          padding: '20px'
        }}
      >
        <Logo {...args} />
      </div>
    )}
  </Story>
</Canvas>
