import { ArgsTable, Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { Card } from 'ui/atoms/card/Card'
import { Typography } from 'ui/atoms/typography/Typography'
import { ThemeProvider } from 'ui/atoms/theme/ThemeProvider'
import { ThemeSwitcher } from 'ui/atoms/theme/ThemeSwitcher'

<Meta
  title="Atoms/Card"
  component={Card}
  parameters={{
    actions: { argTypesRegex: '^on.*' },
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
  decorators={[
    Story => (
      <ThemeProvider>
        <div className="component-story-main">
          <ThemeSwitcher />
          <Story />
        </div>
      </ThemeProvider>
    )
  ]}
/>

# Card

> Communicate _actions_ that users can take.

[![stability-unstable](https://img.shields.io/badge/stability-unstable-yellow.svg)](https://github.com/emersion/stability-badges#unstable)

## Overview

### Description

Card is a component laid out on the page. A card can have a content, a header and a footer. All are optional.

---

<Story
  name="Overview"
  argTypes={{
    header: {
      control: false,
      table: {
        type: { summary: 'Node' }
      },
    },
    content: {
      control: false,
      table: {
        type: { summary: 'Node' }
      },
    },
    footer: {
      control: false,
      table: {
        type: { summary: 'Node' }
      },
    },
  }}
  args={{
    size: 'medium',
    background: 'primary',
    withBorder: true,
    header: <Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
        <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Prim'Holstein</span>
      </Typography>,
    content: <Typography 
        as="div" 
        fontFamily="secondary" 
        fontSize="x-large" 
        fontWeight="xlight"
        color="highlighted-text"
      >MVL</Typography>,
    footer: <div style={{ marginBottom: '4px' }}><Typography as="div" fontFamily="brand" fontSize="medium" fontWeight="bold" color="success">
        <span style={{ textAlign: 'left' }}>Mes vaches laitières</span>
      </Typography></div>,
  }}
>
  {
    args => { 
      return (
        <div style={{ padding: '10px 0 10px' }}>
          <Card {...args} />
        </div>
      );
    }
  }
</Story>

---

### Properties

<ArgsTable story="Overview" />

## Sizes

Size options are `small`, `medium` and `large`.  
Default is `medium`.

<Canvas>
  <Story name="Sizes">
    {args => (
      <div style={{
        display: 'flex', 
        justifyContent:'space-between', 
        flexDirection: 'column', 
        alignItems: 'center'
        }}
      >
        <CardWithTypographies
          contentText='small'
          cardSize='small'
          cardBackground='primary'
          isWithBorder={false}
        />
        <CardWithTypographies
          contentText='medium'
          cardSize='medium'
          cardBackground='primary'
          isWithBorder={false}
        />
        <CardWithTypographies
          contentText='large'
          cardSize='large'
          cardBackground='primary'
          isWithBorder={false}
        />
      </div>
    )}
  </Story>
</Canvas>

## Backgrounds

Available background themes are `primary`, `secondary`, `header`, and `footer`.  
Default is `primary`.

<Canvas>
  <Story name="Backgrounds">
    {args => (
      <div style={{
        display: 'flex', 
        justifyContent:'space-between', 
        flexDirection: 'column', 
        alignItems: 'center'
        }}
      >
        <CardWithTypographies
          contentText='primary'
          cardSize='medium'
          cardBackground='primary'
          isWithBorder={false}
        />
        <CardWithTypographies
          contentText='secondary'
          cardSize='medium'
          cardBackground='secondary'
          isWithBorder={false}
        />
        <CardWithTypographies
          contentText='header'
          cardSize='medium'
          cardBackground='header'
          isWithBorder={false}
        />
        <CardWithTypographies
          contentText='footer'
          cardSize='medium'
          cardBackground='footer'
          isWithBorder={false}
        />
      </div>
    )}
  </Story>
</Canvas>

## Border

Card border is optional.

<Canvas>
  <Story name="Border">
    {args => (
      <div style={{
        display: 'flex', 
        justifyContent:'space-between', 
        flexDirection: 'column', 
        alignItems: 'center'
        }}
      >
        <CardWithTypographies
          contentText='with border'
          cardSize='medium'
          cardBackground='primary'
          isWithBorder={true}
        />
        <CardWithTypographies
          contentText='without border'
          cardSize='medium'
          cardBackground='primary'
          isWithBorder={false}
        />
      </div>
    )}
  </Story>
</Canvas>


export const CardWithTypographies = args => (
  <div style={{marginBottom: '10px', marginTop: '10px'}}>
    <Card          
      size={args.cardSize}
      background={args.cardBackground}
      withBorder={args.isWithBorder}
      header={<Typography as="div" fontFamily="brand" fontSize="small">
          <p style={{ textAlign: 'right'}}>ABC</p>
        </Typography>}
      content={<Typography as="div" fontFamily="brand" fontSize="medium">
          <p style={{ textAlign: 'center'}}>{args.contentText}</p>
        </Typography>}
      footer={<Typography as="div" fontFamily="brand" fontSize="small">
          <p style={{ textAlign: 'left'}}>ABC</p>
        </Typography>}
    />
  </div>
)
