import { ArgsTable, Meta, Story, Canvas, Preview, Props } from '@storybook/addon-docs/blocks'
import { Card } from 'ui/atoms/card/Card'
import { Typography } from 'ui/atoms/typography/Typography'
import { ThemeProvider } from 'ui/atoms/theme/ThemeProvider'
import { ThemeSwitcher } from 'ui/atoms/theme/ThemeSwitcher'

<Meta
  title="Atoms/Card"
  component={Card}
  parameters={{
    actions: { argTypesRegex: '^on.*' },
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
  decorators={[
    Story => (
      <ThemeProvider>
        <div className="component-story-main">
          <ThemeSwitcher />
          <Story />
        </div>
      </ThemeProvider>
    )
  ]}
/>

# Card

> OKP4 UI Card component.

[![stability-unstable](https://img.shields.io/badge/stability-unstable-yellow.svg)](https://github.com/emersion/stability-badges#unstable)

## Overview

Card is a component laid out on the page. A card can have a content, a header and a footer, all optional. 
Content, header and footer must be react components. Any component picked from OPK4 UI library e.g. Typography 
gets styled with colors of the current theme setting.

---

<Story
  name="Overview"
  argTypes={{
    header: {
      control: false,
      table: {
        type: { summary: 'Node' }
      },
    },
    content: {
      control: false,
      table: {
        type: { summary: 'Node' }
      },
    },
    footer: {
      control: false,
      table: {
        type: { summary: 'Node' }
      },
    },
  }}
  args={{
    size: 'medium',
    background: 'primary',
    withBorder: true,
    header: <Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
        <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Prim'Holstein</span>
      </Typography>,
    content: <Typography 
        as="div" 
        fontFamily="secondary" 
        fontSize="x-large" 
        fontWeight="xlight"
        color="highlighted-text"
      >MVL</Typography>,
    footer: <div style={{ marginBottom: '4px' }}><Typography as="div" fontFamily="brand" fontSize="medium" fontWeight="bold" color="success">
        <span style={{ textAlign: 'left' }}>Mes vaches laitières</span>
      </Typography></div>,
  }}
>
  {
    args => { 
      return (
        <div style={{ padding: '10px 0 10px' }}>
          <Card {...args} />
        </div>
      );
    }
  }
</Story>

---

### Properties

<ArgsTable story="Overview" />

## Sizes

Size options are `small`, `medium` and `large`.  
Default is `medium`.

<Canvas>
  <Story name="Sizes">
    {args => (
      <div style={{
        display: 'flex', 
        justifyContent:'space-between', 
        flexDirection: 'column', 
        alignItems: 'center'
        }}
      >
        <Card          
          size="small"
          background="primary"
          withBorder="true"
          header={<Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
              <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Triticum vulgare</span>
            </Typography>
          }
          content={<Typography fontSize="x-large">small</Typography>}
          footer={<Typography fontSize="small" fontWeight="light">Wheat varieties</Typography>}
        />
        <div style={{height: '36px'}}></div>
        <Card          
          size="medium"
          background="primary"
          withBorder="true"
          header={<Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
              <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Triticum vulgare</span>
            </Typography>}
          content={<Typography fontSize="x-large">medium</Typography>}
          footer={<Typography fontSize="small" fontWeight="light">Wheat varieties</Typography>}
        />
        <div style={{height: '36px'}}></div>
        <Card          
          size="large"
          background="primary"
          withBorder="true"
          header={<Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
              <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Triticum vulgare</span>
            </Typography>}
          content={<Typography fontSize="x-large">large</Typography>}
          footer={<Typography fontSize="small" fontWeight="light">Wheat varieties</Typography>}
        />
      </div>
    )}
  </Story>
</Canvas>

## Backgrounds

Available background themes are `primary` and `secondary`. Default is `primary`.

<Canvas>
  <Story name="Backgrounds">
    {args => (
      <div style={{
        display: 'flex', 
        justifyContent:'space-between', 
        flexDirection: 'column', 
        alignItems: 'center'
        }}
      >
        <Card          
          size="medium"
          background="primary"
          withBorder="true"
          header={<Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
              <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Triticum vulgare</span>
            </Typography>}
          content={<Typography fontSize="x-large">primary</Typography>}
          footer={<Typography fontSize="small" fontWeight="light">Wheat varieties</Typography>}
        />
        <div style={{height: '36px'}}></div>
        <Card          
          size="medium"
          background="secondary"
          withBorder="true"
          header={<Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
              <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Triticum vulgare</span>
            </Typography>}
          content={<Typography fontSize="x-large">secondary</Typography>}
          footer={<Typography fontSize="small" fontWeight="light">Wheat varieties</Typography>}
        />
      </div>
    )}
  </Story>
</Canvas>

## Border

Card border is optional.

<Canvas>
  <Story name="Border">
    {args => (
      <div style={{
        display: 'flex', 
        justifyContent:'space-between', 
        flexDirection: 'column', 
        alignItems: 'center'
        }}
      >
        <Card          
          size='medium'
          background='primary'
          withBorder={false}
          header={<Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
              <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Triticum vulgare</span>
            </Typography>}
          content={<Typography fontSize="x-large">without border</Typography>}
          footer={<Typography fontSize="small" fontWeight="light">Wheat varieties</Typography>}
        />
        <div style={{height: '36px'}}></div>
        <Card          
          size='medium'
          background='primary'
          withBorder={true}
          header={<Typography as="div" fontFamily="brand" fontSize="small" fontWeight="light">
              <span style={{ float: 'right', border: 'solid 1px', padding: '4px 8px' }}>Triticum vulgare</span>
            </Typography>}
          content={<Typography fontSize="x-large">with border</Typography>}
          footer={<Typography fontSize="small" fontWeight="light">Wheat varieties</Typography>}
        />
      </div>
    )}
  </Story>
</Canvas>
