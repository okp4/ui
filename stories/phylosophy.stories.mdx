import { Meta } from '@storybook/addon-docs/blocks'
import atomicDesignImage from './atomic-design.png'

<Meta title="Phylosophy" />

# Design system phylosophy

The OKP4 Design System follows the [Atomic Design methodology](https://atomicdesign.bradfrost.com/).

<img
  src={atomicDesignImage}
  style={{
    display: 'block',
    marginLeft: 'auto',
    marginRight: 'auto',
    width: '75%',
  }}
/>

The name Atomic Design comes from the idea of separating the components in:

- atoms,
- molecules,
- organisms,
- templates and
- pages.

This approach helps to build consistent, solid and reusable design systems.
